export const currentUserQuote = (state: any) => state.currentUser.quote;
export const marketsCurrentPage = (state: any) => state.markets.currentPage;
export const marketsQuote = (state: any) => state.markets.quote;
export const marketsRowsPerPage = (state: any) => state.markets.rowsPerPage;
export const marketsItems = (state: any) => state.markets.items;
export const quotes = (state: any) => state.quotes;
